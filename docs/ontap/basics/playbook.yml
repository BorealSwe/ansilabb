---
- hosts: localhost
  gather_facts: false
  name: Connectivity Test and Display ONTAP info

  tasks:
    - name: "Collect ONTAP info"
      netapp.ontap.na_ontap_rest_info:
        hostname: cluster1.demo.netapp.com
        username: admin
        password: Netapp1!
        https: true
        validate_certs: false
        gather_subset: "svm/svms"
      register: info
